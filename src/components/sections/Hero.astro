<section class="hero">
    <div class="hero-meta">
        <span>YERBA BUENA</span>
        <span>EST. 2026</span>
    </div>

    <div class="espresso-line" aria-hidden="true">
        <img src="/assets/espresso-drop.png" alt="" class="portafilter" />

        <span class="drops">
            <span class="drop drop-1"></span>
            <span class="drop drop-2"></span>
            <span class="drop drop-3"></span>
        </span>
    </div>

    <div class="hero-grid">
        <div class="hero-copy">
            <span>SPECIALTY COFFEE</span>
            <p>
                Carefully sourced beans, precise brewing and a quiet space for
                daily rituals.
            </p>
        </div>

        <h1 class="hero-title">
            <span>BREWED</span>
            <span>WITH</span>
            <span>QUIET</span>
            <span>INTENT.</span>
        </h1>
    </div>

    <div class="hero-footer">
        <span>OPEN DAILY</span>
        <span>08:00 — 23:00</span>
    </div>
</section>

<style>
    .hero {
        position: relative;
        min-height: 100vh;
        padding: 2rem clamp(2rem, 6vw, 8rem) 4rem;
        overflow: hidden;

        background: #f7f4ef;
        color: #3b2414;

        --drops-x: 50%;
        --drops-y: 20%;
        --drops-offset: 2px;
    }

    .hero-meta,
    .hero-footer {
        position: relative;
        z-index: 20;

        display: flex;
        justify-content: space-between;

        font-size: 0.72rem;
        letter-spacing: 0.22em;
        text-transform: uppercase;
    }

    .hero-grid {
        position: relative;
        z-index: 6;

        min-height: calc(100vh - 8rem);

        display: grid;
        grid-template-columns: 0.75fr 1.5fr 0.75fr;
        align-items: center;
        gap: 2rem;
    }

    .hero-copy {
        align-self: end;
        padding-bottom: 6rem;
    }

    .hero-copy span {
        display: block;
        margin-bottom: 1.5rem;

        font-size: 0.72rem;
        letter-spacing: 0.22em;
        text-transform: uppercase;
    }

    .hero-copy p {
        max-width: 26ch;

        font-size: 0.95rem;
        line-height: 1.7;
        letter-spacing: 0.02em;

        opacity: 0.68;
    }

    .hero-title {
        position: absolute;
        top: 62%;
        left: 50%;
        z-index: 8;

        margin: 0;

        display: flex;
        flex-direction: column;
        align-items: center;

        font-size: clamp(4.8rem, 10.5vw, 13rem);
        line-height: 0.76;
        letter-spacing: -0.08em;

        font-weight: 900;
        text-transform: uppercase;
        text-align: center;

        transform: translate(-50%, -50%) scale(0.75);
    }

    .hero-title span {
        position: relative;
        z-index: 8;

        display: block;

        animation: heroReveal 1.1s cubic-bezier(0.22, 1, 0.36, 1) both;
    }

    .hero-title span:nth-child(2) {
        z-index: 12;

        font-family: serif;
        font-style: italic;
        font-weight: 400;
        letter-spacing: -0.09em;
        text-transform: none;

        animation-delay: 0.12s;
    }

    .hero-title span:nth-child(3) {
        animation-delay: 0.24s;
    }

    .hero-title span:nth-child(4) {
        animation-delay: 0.36s;
    }

    .espresso-line {
        position: absolute;
        inset: 0;
        z-index: 10;

        pointer-events: none;
    }

    .portafilter {
        position: absolute;
        top: -1.8rem;
        left: 55.5%;

        width: min(52vw, 860px);

        transform: translateX(-50%);

        pointer-events: none;
        user-select: none;

        filter: drop-shadow(0 22px 50px rgba(59, 36, 20, 0.2));

        animation: portafilterReveal 1s ease both;
    }

    .drops {
        position: absolute;
        top: var(--drops-y);
        left: calc(var(--drops-x) + var(--drops-offset));
        z-index: 14;

        width: 3rem;
        height: 54vh;

        transform: translateX(-50%);
    }

    .drops::before,
    .drops::after {
        content: "";
        position: absolute;
        top: 0;

        width: 2px;
        height: 42px;

        border-radius: 999px;

        background: linear-gradient(
            to bottom,
            rgba(239, 166, 70, 0),
            rgba(224, 130, 34, 0.95) 35%,
            rgba(98, 34, 7, 0.9)
        );

        opacity: 0.82;
        filter: blur(0.15px);

        animation: filamentPulse 2.8s ease-in-out infinite;
    }

    .drops::before {
        left: calc(50% - 0.45rem);
    }

    .drops::after {
        left: calc(50% + 0.42rem);
        height: 34px;
        animation-delay: 0.45s;
    }

    .drop {
        position: absolute;
        z-index: 14;

        width: 0.42rem;
        height: 1.35rem;

        border-radius: 50% 50% 58% 58% / 28% 28% 72% 72%;

        background:
            radial-gradient(
                ellipse at 36% 18%,
                rgba(255, 225, 155, 0.9) 0 8%,
                transparent 19%
            ),
            radial-gradient(
                ellipse at 58% 76%,
                rgba(70, 22, 4, 0.75) 0 13%,
                transparent 44%
            ),
            linear-gradient(
                180deg,
                #f2a947 0%,
                #bd5e17 42%,
                #692708 100%
            );

        box-shadow:
            inset -0.08rem -0.18rem 0.22rem rgba(55, 18, 4, 0.45),
            inset 0.06rem 0.08rem 0.12rem rgba(255, 218, 140, 0.55),
            0 0.8rem 1.8rem rgba(59, 36, 20, 0.24);

        transform-origin: 50% 12%;

        animation: dropFall 2.8s cubic-bezier(0.33, 1, 0.68, 1) infinite;
    }

    .drop::before {
        content: "";
        position: absolute;
        left: 50%;
        top: -0.5rem;

        width: 0.12rem;
        height: 0.65rem;

        border-radius: 999px;

        background: linear-gradient(
            to bottom,
            transparent,
            rgba(154, 68, 14, 0.78)
        );

        transform: translateX(-50%);
    }

    .drop::after {
        content: "";
        position: absolute;
        left: 34%;
        top: 18%;

        width: 0.11rem;
        height: 0.32rem;

        border-radius: 999px;
        background: rgba(255, 230, 170, 0.72);

        filter: blur(0.45px);
    }

    .drop-1 {
        top: 24%;
        left: 50%;

        width: 0.28rem;
        height: 1rem;

        rotate: -5deg;
        animation-delay: 0.15s;
    }

    .drop-2 {
        top: 45%;
        left: 59%;

        width: 0.22rem;
        height: 0.72rem;

        opacity: 0.9;
        rotate: 4deg;
        animation-delay: 0.72s;
    }

    .drop-3 {
        top: 62%;
        left: 50%;

        width: 0.55rem;
        height: 1.55rem;

        rotate: -2deg;
        animation-delay: 1.25s;
    }

    .hero-footer {
        position: absolute;
        left: clamp(2rem, 6vw, 8rem);
        right: clamp(2rem, 6vw, 8rem);
        bottom: 2rem;
    }

    @keyframes heroReveal {
        from {
            opacity: 0;
            transform: translateY(0.45em);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    @keyframes portafilterReveal {
        from {
            opacity: 0;
            transform: translateX(-50%) translateY(-18px) scale(0.96);
        }

        to {
            opacity: 1;
            transform: translateX(-50%) translateY(0) scale(1);
        }
    }

    @keyframes filamentPulse {
        0%,
        100% {
            opacity: 0.28;
            transform: scaleY(0.65);
        }

        45% {
            opacity: 0.9;
            transform: scaleY(1);
        }
    }

    @keyframes dropFall {
        0% {
            opacity: 0;
            transform: translate(-50%, -1.3rem) scale(0.52, 0.34);
        }

        14% {
            opacity: 1;
            transform: translate(-50%, -0.15rem) scale(0.72, 1.35);
        }

        38% {
            transform: translate(-50%, 1.7rem) scale(1.04, 0.92);
        }

        72% {
            opacity: 1;
            transform: translate(-50%, 4.5rem) scale(0.9, 1.12);
        }

        100% {
            opacity: 0;
            transform: translate(-50%, 6.6rem) scale(0.68, 1.38);
        }
    }

    @media (max-width: 900px) {
        .hero-grid {
            grid-template-columns: 1fr;
            place-items: center;
            text-align: center;
        }

        .hero-title {
            top: 65%;
            transform: translate(-50%, -50%) scale(0.68);
        }

        .hero-copy {
            padding-bottom: 0;
            justify-self: center;
            text-align: center;
        }

        .hero-copy p {
            margin-inline: auto;
        }

        .portafilter {
            top: 3.5rem;
            left: 56%;
            width: 28rem;
        }

        .drops {
            top: 15.5rem;
            height: 40vh;
        }

        .hero-footer {
            display: none;
        }
    }
</style>